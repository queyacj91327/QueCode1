#include <vector>
using namespace std;
void swap(int &a,int &b);
vector<vector<int>> arrangnment(int start,int end,vector<int> nums,vector<vector<int>> &vet);
void Sort(vector<int>&arrays);
vector<vector<int>> permute(vector<int> &nums);
vector<vector<int>>combination(vector<vector<int>> &vec,vector<int> &nums,int all,int part,int tem[],int count);